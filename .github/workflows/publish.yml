name: Legacy Publish Workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Print migration notice
        run: |
          echo "旧的 main 分支发布入口已停用。"
          echo "请推送版本 tag（例如 v0.1.8 或 0.1.8）触发 Plugin Release Publish。"
